[project]
name = "risovalka"
version = "0.1.0"
description = "Educational 2D game programming engine for kids"
requires-python = ">=3.12"
dependencies = [
    "pyglet>=2.0",
]

[tool.uv]
package = false
